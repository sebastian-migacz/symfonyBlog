

{% block body %}

    <div align="justify" id="navbarArea">


    </div>


        {% for post in posts %}
        <div align="justify" id="postArea">

            <h2>{{ post.title }}</h2>

            <p>{{ post.lead }}</p>

        </div>
        {% endfor %}

    {{ knp_pagination_render(posts) }}
{% endblock %}

{% block stylesheets %}
    <style>
        <link rel="stylesheet" herf="https://maxcdn.bootstrapcdn.com/bootstrap/4.0.0/css/bootstrap.min.css"/>
    </style>
{% endblock %}